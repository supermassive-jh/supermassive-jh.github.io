# 새 블로그 글 등록 규칙

이 프로젝트에서 새 포스트를 추가할 때 지켜야 할 규칙 정리. 프로젝트 전반적인 구조는 [AGENT.md](AGENT.md) 참고.

## 1. 파일 두 곳을 같이 수정한다

1. `docs/{slug}.md` — 글 본문(마크다운)
2. `src/data/posts.ts` — 메타데이터 1건 추가

`slug`는 파일명(확장자 제외)과 정확히 일치해야 한다. `lib/content.ts`가 `docs/*.md`를 파일명 기준으로 읽어서 매칭한다.

## 2. `posts.ts` 필드 규칙

```ts
{
  slug: string       // docs/{slug}.md 와 동일
  title: string      // 아래 "제목 형식" 참고
  excerpt: string     // 목록에 보이는 한 줄 요약
  date: string        // "YYYY.MM.DD"
  category: string     // 사이드바 필터 카테고리 (아래 참고)
  tags: string[]       // 목록/상세에 보이는 태그 pill
}
```

- **날짜**: 실제로 글을 등록하는 오늘 날짜를 그대로 쓴다. 미래/과거 날짜를 지어내지 않는다.
- **카테고리**: 사이드바 필터는 `posts.ts`에서 자동으로 뽑아 쓰므로 별도로 등록할 곳 없음. 기존 카테고리(`JS`, `React`, `아키텍처`, `Spring`, `Next.js`, `배포`)에 맞으면 재사용하고, 안 맞으면 새로 만들어도 됨.
- **정렬**: 목록은 `date` 내림차순 + 같은 날짜는 `posts.ts` 배열에서 더 아래(나중)에 있는 항목이 위로 온다. 즉 같은 날 여러 편을 한 번에 올릴 때는 **읽는 순서대로(1편→N편) 배열에 추가**하면 목록엔 자동으로 N편→1편 순으로 뜬다.

## 3. 시리즈 제목 형식

여러 편으로 나누는 글은 이 형식을 쓴다:

```
{큰 주제} ({번호}). {세부 제목}
```

예: `상태관리 원리 파헤치기 (1). 클로저와 메모리 누수, 그리고 GC`

- 번호는 **읽는 순서**이고, **가장 큰 번호가 가장 최신 글**이어야 한다(목록 정렬 규칙과 일치시킬 것).
- 시리즈가 아닌 단발 글은 번호 없이 제목만 쓴다.

## 4. 마크다운 본문 작성 규칙

- **글 문체**: 노트/강의안식 개조식이 아니라 1인칭 회고 블로그 톤("~했다", "~였다"). 원본 자료(대화 로그, 정리 노트 등)를 그대로 옮기지 말고 블로그 글로 다시 쓴다.
- **제목 중복 금지**: `PostPage.tsx`가 `post.title`을 자체 `<h1>`으로 렌더링하므로, 마크다운 본문 맨 위에 `# 제목`을 또 넣지 않는다. 본문은 바로 문단이나 `##` 소제목으로 시작.
- **시리즈 연결**: 이어지는 글이면 "지난 글에서/다음 글에서는"으로 자연스럽게 연결.
- **내부 링크**: 다른 글로 링크할 땐 `[텍스트](/post/그-글-slug)` 형식(절대 경로, `#` 없음). `PostPage.tsx`의 커스텀 렌더러가 `/`로 시작하는 링크를 자동으로 React Router `Link`(새로고침 없는 이동)로 바꿔준다. 외부 링크는 그냥 일반 URL을 쓰면 자동으로 새 탭으로 열린다.
- **코드블록은 반드시 언어 태그를 붙인다** (` ```java `, ` ```ts ` 등). 언어 태그가 없으면 syntax highlighting이 적용되지 않고 inline code로 오인될 수 있다. 다이어그램처럼 진짜 언어가 없는 코드블록은 ` ```text `를 쓴다.
- **지금 색깔이 입혀지는 언어**: `java`, `javascript`, `jsx`, `html`, `sql`, `ts`, `tsx` (`src/components/code-block.tsx`에 등록됨). 이 목록에 없는 언어를 쓰면 색은 안 입지만 깨지지는 않는다(일반 코드로 표시). 새 언어가 필요하면 `code-block.tsx`에 `react-syntax-highlighter/dist/esm/languages/prism/{언어}`를 import해서 `registerLanguage` + `supportedLanguages` set에 추가.
- **표(GFM 테이블)**는 자유롭게 써도 된다 — 좁은 화면에서는 자동으로 그 표 안에서만 가로 스크롤되도록 처리되어 있음(페이지 전체가 밀리지 않음).

## 5. 등록 후 확인

```bash
pnpm run build          # 타입 에러, sitemap.xml 생성 확인
pnpm run format          # 새 파일 포맷팅
pnpm run lint
```

가능하면 dev 서버 띄워서 목록에 새 글이 올바른 순서/카테고리로 뜨는지, 상세 페이지가 정상 렌더링되는지, 모바일 너비에서 가로 스크롤이 생기지 않는지 확인한다.
